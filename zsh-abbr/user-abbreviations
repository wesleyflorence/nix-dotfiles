abbr "bunrun"="bun run dev -- --open"
abbr "concat-repo"="git ls-files | grep -v '\.lock' | xargs grep -I -l '' | xargs tail -n +1"
abbr "gg"="git status"
abbr "hms"="home-manager switch"
abbr "jrn"="nvim ~/vault/jrn/2023.10.21.md"
abbr "jrn-full"="cat ~/vault/jrn/*.md | nvim -c 'set filetype=markdown'"
abbr "ll"="exa -al"
abbr "ls"="exa"
abbr "nd"="nix develop"
abbr "vim"="nvim"
